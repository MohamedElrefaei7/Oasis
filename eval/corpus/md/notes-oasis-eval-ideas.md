# Eval ideas — scratch notes
- query set should include date-filtered queries ("meeting notes from last year")
- folder filter queries once corpus has subfolders
- near-duplicate discrimination: contract v1 vs final
- keyword-only queries with FTS5 operators
